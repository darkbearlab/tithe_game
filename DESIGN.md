# TITHE — 設計文件

> **每十年，教廷抹一名騎士以聖血，送他跨進地獄。**
> **他若走到盡頭，人間再安穩十年。他若死了，你為他的兒子命名。**

單一英雄・俯視・動作 roguelite。承襲 [`darkbearlab/Homeward-`](https://github.com/darkbearlab/Homeward-) 的引擎，
但**不是它的變體**——戰鬥迴圈、流程結構、敘事層全部重寫。要搬什麼砍什麼見 [`HARVEST.md`](HARVEST.md)。

## 目錄

- [1. 三個參照](#1-三個參照)
- [2. 戰鬥：資源迴圈](#2-戰鬥資源迴圈)（含 [2.8 難度契約](#28-難度契約)）
- [3. 移動](#3-移動)
- [4. 敵人名冊的設計律](#4-敵人名冊的設計律)
- [5. 流程：層與房間](#5-流程層與房間)
- [6. 成長：掉落即是 boon](#6-成長掉落即是-boon)
- [7. 數字要飛](#7-數字要飛)
- [8. meta：只有兩條軸](#8-meta只有兩條軸)
- [9. 血脈紀年](#9-血脈紀年)
- [10. 父輩：被地獄佔領的先祖](#10-父輩被地獄佔領的先祖)
- [11. 明確不做的事](#11-明確不做的事)
- [12. 待決清單](#12-待決清單)

---

## 1. 三個參照

| 面向 | 參照 | 取什麼 |
|---|---|---|
| **戰鬥** | DOOM 2016 | push-forward：資源只從敵人身上來，唯一的活路是往前打 |
| **防禦的形狀** | Sekiro | 招架不是為了活下來，是為了**累積架勢直到處決**——防禦本身就是進攻 |
| **流程** | Hades | 一房一選擇，選的是「你要什麼」不是「你要去哪」 |
| **掉落** | Diablo | 密度。地上一直在發光 |
| **敘事** | — | **血脈紀年**：每個 run 是一代人，父傳子，禱文一代比一代長 |

不取的東西同樣重要：不取 DOOM 的武器輪盤複雜度（我們是近接為主）、不取 Hades 的厚重 meta（見 §8）、
不取 Diablo 的數值長跑（一個 run 就結束）。

---

## 2. 戰鬥：資源迴圈

### 2.1 核心

DOOM 2016 的核心不是「打得爽」，是**你永遠缺某樣東西，而取得它的唯一辦法是進攻**。
每一個防守動作都在讓你更缺，每一個進攻動作都在補回來。這是整個設計的地基。

**兩條資源，兩條都只從敵人身上來：**

| 資源 | 是什麼 | 花在哪 | **唯一**的來源 |
|---|---|---|---|
| **聖血**（HP） | 命 | 挨打 | **處決**踉蹌中的敵人 |
| **怒火** | 容錯的價格 | 格擋・奔跑・技能・大絕招 | **命中**（小）・**招架**（中）・**處決**（大） |

外加一件不是「資源」而是「按鈕」的東西：**聖油**（§2.4）。

> **名字**：你給了「怒火／仇恨」兩個選項，文件統一用 **怒火**。
> 「仇恨」在遊戲術語裡通常指 aggro／仇恨值（誰在打誰），會撞名。
> 若之後想要更貼教廷的味道，`聖怒` 也可以，換的只是字。

### 2.2 怒火：開場是滿的，只會被時間磨掉

> 聽完禱文、想起那個不曾回來的父親，騎士是**滿身怒火**跨進地獄的。

- **開場滿。** 這是敘事，也是節奏——第一房不需要暖機。
- **攻擊不花怒火。** 這是 push-forward 成立的前提——你永遠打得動。
- **怒火不隨時間回復，只隨時間衰退。** 這一條是整個設計的成敗點。
  方向很重要：
  - 時間**回復** → 等待有收益 → 迴圈變成「格擋 → 退開 → 等回來 → 再上」，
    「站著不打」會用另一個名字回來 ❌
  - 時間**衰退** → 等待永遠是虧的 → 想留住怒火只有一條路：**繼續打** ✅

  訊息因此很清楚：**長時間不出手＝慢性繳械。**
- **衰退只在戰鬥中發生**（房裡還有活著的敵人時）。**清場那一刻凍結**，
  你帶著剩下的怒火跳進下一房。

  **換房不另外扣。** 一來那是純粹的 feel bad；二來如果清場後還在扣，
  認真撿東西、慢慢逛的玩家會被懲罰——懲罰仔細是最糟的一種扣款。
  打得快的人帶著高怒火進下一房，那是**獎勵**，不是漏洞（怒火有上限，攢不起來）。

三個層級的回充，對應三種技術水平：

| 動作 | 回怒火 | 意義 |
|---|---|---|
| 命中 | 小 | 保底：只要你還在揮，就不會完全枯竭 |
| **招架** | 中 | 獎勵讀招 |
| **處決** | 大 | 和回血是同一個動作＝處決是雙重補給 |

### 2.3 怒火的三個花法，和一個張力

- **格擋**（按住）：純減傷，不給體幹傷害、不給資源。安全網。
- **奔跑**（按住）：房間內的快速轉移／風箏。
- **技能**（`1`~`N` 自由位）：**全部改吃怒火，不要 CD。** 玩家只看一條資源條，
  沒有 CD 表要記，而且技能的施放頻率由你打得多兇決定——**技能因此也被接進迴圈裡**。
- **大絕招**（`Q`；`空白` 讓給了 dash）：怒火過門檻 → 清空整條換一發大的。
  門檻（`CONFIG.wrath.ultCost`）**刻意低於上限**：怒火隨時在衰退，門檻若等於上限，
  你就只有「剛好回滿的那一幀」按得下去＝實際上放不出來。
  **開場滿的連帶效果：你一進地獄就有一發大絕招可以放。** 這是好事——
  滿身怒火跨進去、第一房就能直接炸，開場一分鐘內有個爆點。
  （若之後希望大絕招要「賺」，就得讓門檻高於初始值。不建議。）

**怒火高 → 減傷。** 敘事上是「盛怒中的騎士感覺不到痛」。

這條規則和「格擋吃怒火」疊在一起會產生一個很好的張力：
**格擋救了你這一下，但花掉的怒火讓你之後更脆。**
所以格擋不是免費的保險，它是一筆借款，你得趕快打回來還。

> ⚠️ **這條規則的形狀本來是死亡螺旋**（順的時候更順、卡的時候更卡）。
> 它之所以安全，**完全是因為怒火靠打人回復**——低怒火會逼你進攻，進攻就回怒火，系統自我修正。
> 一旦哪天改成時間回復，這條減傷規則就必須同時拿掉。兩者是綁在一起的。

**張力來自花費，不是來自衰退。** 開場滿之後，怒火有沒有壓力完全取決於
格擋／奔跑／技能定價得夠不夠兇。調值目標寫成一句話：

> 大部分時間應該在**中段徘徊**，偶爾見底。
> 如果整場都貼著上限，那是花費太便宜，不是衰退太慢。

衰退速度只負責傳達「不出手會慢慢失血」這個訊息，**不負責製造稀缺**。
（永遠的保底手段是**免費的 dash**，見 §3。）

### 2.4 處決（Glory Kill）——最重要的一件事

**這套機制在 Homeward 已經寫好了九成。** 現有的體幹（poise）系統：

- 每次命中扣體幹，扣光＝**體幹破** → 大硬直（`staggerT`）+ 受傷 ×1.6 + 大幅擊退 + 紅色爆擊火花
- 觸發點：`applyPoise()` → `breakPoise()`（`index.html` ~4088–4109）
- 已經有事件掛勾：`Hooks.fire('poiseBroken', { u, by })`
- Homeward 的 README 自己就管這個狀態叫「**處決窗**」

要加的只有最後一哩：

1. 踉蹌中的敵人**視覺上要跳出來**（現在只有一次性火花 → 改成持續標記，遠遠一眼看得到「那隻可以吃」）
2. 貼近 + 按鍵 → **處決**：短促、短無敵、鏡頭微推、頓幀，然後**噴聖血 + 大量怒火**
3. 回收量與敵人體型掛鉤（雜兵一小口、重甲一大口）

> **設計律：體幹只有敵人有。**（實測後定案）
> 體幹在這套設計裡只有一個工作：把敵人推進踉蹌 → 開處決窗 → 讓玩家把血和怒火拿回來。
> 它是**進攻的計量器**，不是防禦的計量器。騎士自己沒有體幹——
> 他挨打的代價是掉血，他被定住的唯一途徑是**破防**（舉盾但怒火付不出這一擊）。
> 這讓硬直的意思很乾淨：你不是「挨太多下」，你是「擋不起了」。
> HUD 也因此只剩一條怒火條——兩條會漲會退的條擺在一起，讀不出是誰在說話。
>
> **設計律：處決是唯一的回血手段。** 一旦破例（掉落回血包、技能回血），push-forward 就塌了。
> 技能可以回血——但技能燒怒火，怒火只能打出來。迴圈仍然閉合在敵人身上。

### 2.5 聖油：一顆刪除菁英的按鈕

**極少次數**（一趟一兩管），對**菁英／大型敵人**使用＝**當場淨化，噴滿地資源**。

它不是持續性資源，是一個**多巴胺尖峰 + 恐慌按鈕**：

- 次數要**永遠顯示**在畫面上（你要一直意識到自己還有幾管）
- 使用的那一刻要**大**：頓幀、光爆、螢幕震、資源噴泉（聖血 + 怒火同時噴到滿地）
- 對雜兵使用＝浪費（不禁止，但明顯不划算），它的用途是「那隻我打不動的東西」

> 和原案的差別：原本設計成「清雜兵的保底閥」（DOOM 電鋸的位置）。
> 改成**刪菁英**之後結構性任務讓給了怒火（怒火靠命中回，所以永遠不會枯竭到動不了），
> 聖油變成純粹的爽點——而那正是它該做的事。

### 2.6 盾、格擋與招架：**留**

DOOM 沒有防禦鍵，但 DOOM 是射擊遊戲，它的防禦是三維空間裡的走位。
俯視近接每一幀能表達的移動遠比那少，而且你常常就貼在接觸距離上——防禦只剩 dash 太薄。

關鍵是讓它們**餵迴圈，而不是暫停迴圈**：

| | 角色 | 給什麼 | 吃什麼 |
|---|---|---|---|
| **招架**（窄窗、讀招） | **迴圈的加速器** | 大量**體幹**傷害 → 快速逼出踉蹌 → 接處決；回中量怒火 | 空招自己硬直（現有機制） |
| **格擋**（按住） | **安全網** | 純減傷，**不給體幹傷害、不給資源** | 怒火 |

這樣分，turtle 依然是慢性死亡（格擋不產出任何東西，舉著盾就是在餓死自己），
而「我讀對了這一下」有明確且巨大的回報：**招架是通往處決的快速道路，處決是唯一的回血**——
技術直接換成續航。

護欄 Homeward 早就做好了，全部沿用：護弧只罩正面（`blockArcDeg`）、
盾的三角取捨（小圓盾窄弧寬窗 / 鳶盾寬弧難招架）、**舉盾時怒火不足擋下＝直接破防**
（那條規則在新設計裡是防止長按盾的那把鎖，要留）。

騎士沒有體幹之後，破防是他**唯一**會被打進硬直的方式（`guardBreak()`）。
這是刻意的收斂：硬直只有一個原因，玩家不用猜自己為什麼不能動。

### 2.6b 主副手＝左右鍵（使用者定案）

**左鍵是主手的招式，右鍵是副手的招式。** 武器不再是「一組揮擊參數」，而是**一袋招式**：

```
spear:  { light: 突刺（快、長）,  heavy: 橫掃（慢、廣） }
axe:    { light: ...,             heavy: 慢、hyperArmor、體幹傷極高 }
sword:  { light: ... }            // 單手只有一招，看它在哪隻手就綁哪個鍵
shield: { hold: 格擋,             tap: 招架 }
```

`startSwing(u)` 因此變成 `startSwing(u, move)`，參數從**招式**讀而不是從武器讀。
這其實是簡化：`hyperArmor` / `swingStyle` / `backstabMul` 這些散在武器上的旗標全部收進招式，
以後加「上挑」「衝刺突刺」不用再開新欄位。

副手槽因此變成一張真正的取捨表——**招架掛在副手上**：

| 副手 | 左鍵 | 右鍵 | 放棄了什麼 | 怒火從哪來 |
|---|---|---|---|---|
| **盾** | 主手輕擊 | 按住＝格擋／點按＝招架 | 輸出 | 招架（中量）＋花怒火買安全 |
| **空手** | 主手輕擊 | **徒手招架**（窄窗、空揮自己硬直） | 格擋。只有讀對招才活 | 招架，賭性最高 |
| **第二把單手** | 主手 | 副手 | **防禦整個沒有** | 命中量堆出來 |
| **雙手武器**（佔兩手） | 輕擊 | **重擊** | **防禦整個沒有** | 體幹傷直接開處決窗 |

四個選擇沒有一個是嚴格最優，而且它們對應四種不同的怒火收支——
**四條路通向同一個處決**，這正是 §2 要的。
（弓弩天然是雙手：左鍵射、右鍵瞄；現有的 `isScoping` 就是它的「重擊」。）

連帶死掉的東西（已執行）：`X`／滾輪換武器組、第二組手部 set、`CONFIG.weaponSwap`、
meta 升級「換手」、`slotsOf` 的 2 格規則，以及 **`weight` 欄位**——
它的三個消費者（奔跑懲罰、抽出時間、倉庫格數）到此全部消失，欄位已從所有武器／甲／盾刪乾淨。

敵人吃同一份 `WEAPONS`，牠們只用 `light`。**重擊給不給菁英是開放問題**——
傾向給，「這隻會做你會做的事」在敵人設計上很值錢，也是 §10 父輩 boss 的地基。

### 2.7 攻擊權（`attackTokens`）：**刪除**

現有機制是「同時只有 2 隻近接敵能進攻，其餘在外圍繞行」。那是黑暗騎士／刺客教條式的近戰禮貌，
用來讓 1 打多在**慢節奏**下可解。

DOOM 的壓力來源相反：**所有東西都在攻擊你，同時**。之所以可解，是因為每一發都**看得見、躲得掉**。
所以：拿掉配額，壓力改由「可見可躲的投射物 + 有預告的近接突進」承擔。

現有的**攻擊預告**（近接扇形 / 遠程長方形彈道走廊，填滿的瞬間＝落下的瞬間）正是為這件事寫的，
全部留下並且**加強**——它從「輔助資訊」升格成戰鬥的主要語言。

### 2.8 難度契約

這是調值時的最高指導原則，凌駕所有個別數字。

> **騎士的性能必須足以打贏全遊戲最難的挑戰——即使完全沒有任何 meta 解鎖。**
> **但在那個難度下，他完全不能犯錯。**
>
> **地獄的爪牙也必須真的強得起這個名號。**

四條可執行的推論：

**1. 這條契約依賴「meta 不給數值」（§8）。**
一旦有數值升級樹，難度就必須以「假設玩家升到第幾級」為基準——
於是第 1 個 run 打不贏、第 50 個 run 沒挑戰。**因為我們沒有那棵樹，難度目標才能是固定的。**
兩個決定是綁在一起的：誰想加回數值 meta，就等於在廢掉這條契約。

**2. 每一次受傷，都必須是玩家指認得出來的失誤。**
這是「不能犯錯」的可測形式。如果玩家挨了一下卻說不出自己哪裡做錯，
那是遭遇設計的 bug，不是難度。因此禁止：無預告的攻擊、畫面外的攻擊、
反應時間不足的攻擊、隨機的無法迴避傷害。

騎士的工具（dash 的 i-frame、招架、處決、大絕招）**必須能回答名冊裡的每一種攻擊**。
新敵人的驗收條件之一：它的每一招都有一個不受傷的解法。

**3. 敵人的威脅來自傷害與速度，不是血量。**
高血量沙包同時違反兩件事：它拖慢「擊殺 → 資源」的迴圈（§2.1），也讓戰鬥變成磨。
**地獄的爪牙應該是「殺你很快、也死得很快」**——這才是 DOOM 的形狀。
（唯一的例外是菁英，而那正是聖油存在的理由。）

**4. 越後面的層變難，靠的是組合，不是數值膨脹。**
不加 HP、不加傷害倍率。變難的方式是：更惡毒的敵人組合、更擠的空間、更少的喘息。
數值膨脹會直接毀掉推論 1 的固定難度目標。

> 這條契約和 §2.7（拿掉攻擊權配額）互相加碼：騎士很強、敵人很致命、所有人同時進攻，
> 唯一撐得住這個組合的東西是**可讀性**。所以攻擊預告不是輔助功能，是這個遊戲的地基。

---

## 3. 移動

「絲滑」不是形容詞，是四個具體數字：

| 項目 | Homeward 現況 | TITHE |
|---|---|---|
| 基礎移速 | 110 px/s（註解寫「戰術化：放慢」） | **顯著調高**（先試 190–220） |
| 整體節奏 `CONFIG.tempo` | **1.2（＝刻意拉長 20%）** | **回到 1.0 或更低**（0.85–0.9） |
| 加速度 / 摩擦 | 無（瞬間到達目標速度） | 保持瞬間感，加**極短的減速尾巴**讓轉向不黏 |
| 奔跑 | Shift 長按、吃耐力、被負重懲罰 | Shift 長按、**吃怒火**、**負重懲罰拿掉**（固定 1.9×） |
| **轉向** | 每把武器一個 `turnSpeed`（短刀 660 → 戰鎚 360） | **騎士沒有轉速上限**：滑鼠指哪就朝哪，1:1 |
| **dash** | 無 | **新增**：短距離、CD 制、**完全免費**、有 i-frame |

**dash 免費是刻意的。** 防禦不該有成本，進攻才有回報——而且進房那一刻你怒火為零，
免費的 dash 是你唯一的保底。付費的防禦（格擋／奔跑）是你打出來之後才買得起的升級。

負重對移動的懲罰整條拿掉（`sprintMult` / `carriedWeight` 那條線）。草叢減速改成固定值。

**轉向上限也拿掉了**（使用者定案）。武器的差別靠前後搖 / 數值 / 詞條去分，
不靠「這把轉得慢」——那只是隔一層讓操作變鈍，而且它和高速移動直接打架：
跑得動但轉不過來，手感會在跟你吵架。

只剩兩個轉向限制，兩個都不是武器屬性：

- **揮出去之後**（`CONFIG.melee.swingTurnRate`，判定窗＋後搖）：揮擊是一個承諾，
  不能揮到一半整個人轉過去掃，否則刀刃會變成雷達、揮擊弧的設計就沒有意義。
- **敵人**（`CONFIG.enemy.turnSpeed`，逐兵種可覆寫 `turn`）：**刻意保留**。
  敵人轉得慢才有「繞到他背後」這個玩法（flank/backstab 都看目標的 facing），
  也才讀得出遠程預告鎖死朝向的那一下。這是敵人的可讀性，不是玩家的手感。

---

## 4. 敵人名冊的設計律

> 「不是無雙割草，而是不同個性鮮明的敵人穿插出現。」

這句話展開就是 DOOM 的敵人設計律。訂成硬規則，任何新敵人都要通過：

1. **每一隻問一個不同的問題**，而且有一個「正確答案」（某個武器、某個位置、某個時機）
2. **每一隻有唯一的空間要求**（逼你站到某處）**或**唯一的時間要求（逼你在某刻動）
3. **沒有任何一隻可以被「站著平砍」解決**——如果可以，牠不該存在
4. **同時在場 8–15 隻，不是 40 隻**。壓力來自**組合**，不是數量
5. **每一隻都是資源**。沒有純麻煩的敵人；擋路的東西同時是補給
6. **殺你很快，也死得很快**（§2.8 推論 3）。威脅來自傷害與速度，不是血量
7. **牠的每一招都要有一個不受傷的解法**（§2.8 推論 2）。這是新敵人的驗收條件
8. **遠程只有兩種原型，沒有中間態**（見 §4.1）

律則 4 對引擎是好消息：不必為湧怪重寫，現有的單位規模就夠。

### 4.1 投射物的兩種原型（使用者定案）

每一種遠程攻擊只能是這兩者之一：

| | **A 彈幕型** | **B 狙擊型** |
|---|---|---|
| 預告窗 | **沒有** | **有**（長預告走廊） |
| 預告中會不會追瞄 | — | **不會**，朝向完全鎖死 |
| 彈速 | **很慢** | **近乎 hitscan** |
| 可見度 | **極高**（發光的球 + 拖尾） | 亮痕（每一發都留） |
| 你躲的是 | **彈本身**——空間題 | **預告**——時間題 |
| 失誤讀作 | 「我沒閃開那顆球」 | 「我沒離開那條線」 |

**中間態是禁止的**：會追瞄的快彈＝無法迴避的傷害，那是設計 bug，不是難度
（違反 §2.8 推論 2「每一次受傷都必須是玩家指認得出來的失誤」）。

實作上：`aimTime: 0` + 低 `projSpeed` + `orb` ＝ A；長 `aimTime` + 高 `projSpeed` + `tracer` ＝ B。
`CONFIG.ranged.aimTurnMul` 預設 **0**（鎖死），要追瞄的武器得自己把它調上去——
**預設不該是那個危險的那邊**。

### 初版名冊（草案，7 隻）

| # | 名 | 行為 | 牠問的問題 | 正確答案 |
|---|---|---|---|---|
| 1 | **膿仔** | 慢、弱、成群、不痛 | 無（牠不是威脅） | **牠是行走的補給站**——缺血缺怒火的時候回去吃 |
| 2 | **撲咬者** | 極快直線突進，有預告線，撲空後有硬直 | 你能不能在移動中側閃 | dash 或走位側移，吃牠的硬直 |
| 3 | **投火者** | 遠程拋物線，落點預告圈 | 你能不能**不站定** | 持續移動；優先清掉（牠讓你不能站著處決） |
| 4 | **盾衛** | 正面免疫（沿用現有裝甲第一層 + 反射舉盾） | 你能不能在混戰中繞到背後 | 繞背（現有 `flank` 無視雙層裝甲的機制直接可用） |
| 5 | **嚎叫者** | 不攻擊，但持續強化／召喚附近敵人 | 你能不能**放棄眼前的目標**去處理優先級 | 立刻衝過去殺掉，即使代價是背對別人 |
| 6 | **重甲**（菁英） | 高血高甲、慢、近身重擊、擊退強 | 資源管理：你打不動牠 | 破甲手段；或**燒一管聖油刪掉牠** |
| 7 | **裂體者** | 死時分裂成兩隻小的（小的再裂成膿仔） | 清場順序：你要在什麼時機殺牠 | 留到最後，或用範圍一次收乾淨 |

**Homeward 現有兵種可直接映射**（`UNIT_CLASSES` / `ENEMY_TYPES` 的表結構原樣沿用，只換資料）：
`levy`→膿仔、`wolf`→撲咬者、`archer`/`arbalest`→投火者、`shieldman`→盾衛、`manatarms`/`serjeant`→重甲。
嚎叫者與裂體者是新行為，要新寫。

### AI 的形狀要換

現有 AI 是**戰術 waypoint 制**（`scoreNode` 依標籤/暴露/側翼評分選點、佔點 `reservation`、遲滯、
巡邏認領、搜索焦點）。那是為「潛行 + 掩體 + 少量敵人」寫的，在封閉房間裡完全用不上。

換成**直接操舵**（steering）：朝目標移動 + 避開彼此 + 各兵種自己的位移花招（繞圈 / 突進 / 保持距離）。
這一換順便解掉「敵人只能走手刻 waypoint 圖」的限制——**房間可以程序生成了**。

---

## 5. 流程：層與房間

### 5.1 房間

- **封閉房間**。進門 → 鎖上 → 清場 → 出口開啟。
- **沒有撤離點、沒有進出點、沒有潛行。** 房間是亮的，你看得到所有東西。
- 房間大小約現有地圖的 1/2 ~ 2/3，重點是**沒有死角、沒有可以龜的地方**。

### 5.2 迷霧與視野：**預設關，但程式留著**

一般房間**不使用**視錐與戰爭迷霧——你進的是一個已經被驚動的封閉空間，
所有東西都醒著、門是鎖的，藏沒有意義，而視野遮蔽只會妨礙高速戰鬥的閱讀。

但**整套機制留在程式裡，用旗標關掉**（Homeward 已有 `arenaNoFog` 的先例），
留給**暗房**這種特殊房型：只有你視錐內看得見，其餘全黑。偶爾出現一次會非常有效。

> 附註：迷霧關掉時它只是**視覺層**。一般房間裡敵人本來就全部醒著（門鎖了、你在裡面），
> 不需要「看見才追」的判定。暗房才兩邊都套上。

### 5.3 出口：房間中央的洞

清場那一刻，**房間中央地板碎裂，一個洞打開**（光柱衝上來）。跳下去＝下一房。

只有一個出口，**選擇不在「走哪扇門」，在「跳下去之前挑什麼獎勵」**：洞開啟時彈出**三選一**。

為什麼比 Hades 的兩扇門好：

- **旋轉跟隨 + 貼身視野下，「兩扇門在畫面兩側」的空間閱讀其實很差**——你根本看不到全景
- **「往下墜」在地獄主題裡是對的**。你是一路向下的，descent 直接被視覺化
- 地板碎裂比「門開了」是更明確、也更爽的「這一房結束」信號
- 少一個「門要放哪」的地圖約束 → **房間程序生成更自由**

### 5.4 獎勵：**全揭露**

三個選項**完整顯示內容**——具體是哪把武器、什麼詞條、什麼稀有度，看得一清二楚。

不做「只顯示類別、選了才開箱」。多巴胺來自「我在三個好東西裡挑一個」，
不是「我賭對了嗎」——而且賭錯會讓人想 save scum。

選項類別（草案）：⚔ 武器 · 🛡 防具 · ✨ 技能／術 · 🔥 聖油 · ❤ 聖血 · **⚰ 父親**（見 §10）

### 5.4b 整備：跳下去之前的那一停（使用者定案）

**戰鬥中不能換武器。** 進房那一刻手上是什麼就是什麼——換裝是一個**選擇**，不是一個按鍵。
`X` 換武器組（兩套主副手隨時互換）是 Homeward 的遺產，在 push-forward 裡它是純粹的
正面效益（沒盾時切出盾、打不動時切出鎚），沒有代價、沒有取捨，只是多按一個鍵。

換裝挪到**跳下去之前**，順序與規則：

1. **先三選一**（§5.4），**再進整備**。獎勵先落袋，才知道自己在配什麼。
2. **整備是暫停**，不限時。§5.4 已經承諾了「思考在房間之間，執行在房間之內」——
   加倒數會逼玩家用手速做本來該用腦袋做的事，和全揭露自相矛盾。
3. 一次停頓做完兩件事。節拍因此是乾淨的：**打 → 停一次（選＋配） → 打**。

### 5.4c 波次：一間房不是一批人（使用者定案）

一間房**不會**是「殺完六個人就結束」。它是好幾波，**每一波從不同的門進來**——
清光一波 → 別的門亮起來 → 湧出下一波 → 最後一波清光才開洞。

這樣一間房才有形狀（開場 → 加壓 → 收尾），而且 §1 那句
「不是無雙割草，而是不同個性鮮明的敵人穿插出現」本來就是在要求波次：
一次把所有人放進場，玩家只會看到一團；分波才讀得出誰是誰。
怒火的收支也因此跨越整場，而不是每一小包各自結算。

> ⚠️ **這不是 Homeward 的追兵。** 那套是「時間到了就無限湧出」的懲罰時鐘，方向和 TITHE 相反：
> 清場後的閒逛已經由**怒火衰退**定價（§2.2），再加一個無限時鐘只會逼玩家速通，
> 把「房間之間才做決策」（§5.4）整個壓掉。
> 拿回來的是它的**管線**（從別的點生成、直接 ENGAGED），不是它的**觸發條件**：
> 清光一波才來下一波，而且**波數是有限的**。

**門一定要先亮起來再湧出。** §5.1 說房間沒有死角——背後無預警冒出一波是廉價的偷襲，
不是難度。門的位置取「離騎士最遠的錨點」：不是為了公平，是為了讓你有時間看到它、
轉過去、決定要不要衝上去堵門口。

### 5.5 層（biome）

四層地獄，每層 8–12 房 + 層王。層與層之間換配色、換敵人組合權重、換場景元素。
最後是**地獄之王**。

**單一出口讓「路線規劃」消失了**——沒有分岔，只剩每一房的短期選擇。
便宜的補法：**每層開頭報一次「本層 X 房，盡頭是 OO」**。不用地圖，一行字就夠，
你就知道還剩多少、什麼時候該存資源給層王。

**目前實作**：`CONFIG.floor.rooms` 間房一層，房間是 `CONFIG.floor.pool` 的**手刻輪播**，
落地直接進下一間房——**路線圖在這條路徑上完全不出現**。層王還沒做，現在是無限往下。
程序生成房間要先把編輯器的「鋪滿網格點 + 自動整理連線」搬進遊戲端（尋路要 waypoint 圖）。

---

## 6. 成長：掉落即是 boon

### 6.1 張力

「裝備噴得和垃圾一樣多」和「DOOM 的節奏」直接衝突：**在戰鬥中停下來比較裝備數值會把節奏殺死。**

### 6.2 兩層分離

| 層 | 在哪 | 是什麼 | 玩家動作 |
|---|---|---|---|
| **爽度層** | 戰鬥中，滿地都是 | 聖血、怒火、暫時性增益 | **走過去就吃**，零決策、零暫停 |
| **決策層** | 清場後，洞口 | 裝備／技能，**三選一，全揭露** | 停下來看，但戰鬥已經結束了 |

「噴得像垃圾」的爽度發生在戰鬥裡（滿地發光、數字亂飛），
「這一趟我在 build 什麼」的決策集中在洞口——**兩者都不打斷對方**。

### 6.2b 沒有背包（使用者定案）

**身上的留著，袋子裡的活不過那道門。**

- 戰鬥中撿到的東西進一個**臨時暫存**，到整備畫面（§5.4b）只能**裝到身上或丟掉**。
- 跨房不保留暫存。沒有倉庫、沒有跨 run 的庫存——§8 的 meta 只有兩條軸，這裡不能有第三條。
- **不在暫存裡的東西**：聖油（獨立管數，連 `1`~`4` 都不佔）、已裝在 `1`~`4` 的技能／術／消耗品。
  這些是「身上」，跟血一樣是狀態。

三個白賺的連帶效果：

1. **撿東西可以完全自動化**——沒有容量壓力、也沒有「現在要不要撿」，走過去就吸走。
   整理背包因此被徹底趕出房間：房間裡你只做一件事，打。
2. **C7／C8／C11 一大塊直接失去作用對象**（休息關、倉庫 UI、事件經濟、格子規則）。
3. 規則短到可以寫在載入畫面上。

> 已知風險：每道門都是強制決策，一層 8–12 房就是 10 次「這把比我身上的好嗎」。
> 真的變累的話**解法不是加袋子，是降低掉落密度、提高單件重要性**——
> 門口三選一才是主要獲得管道，地上撿的是零頭。（使用者判斷：靠顏色與詞條多半一眼就分得出來，先不處理。）

### 6.3 沿用現有資產

- `WEAPON_QUALITY`（精良度 1–5 ＝強化格數預算）+ `AFFIXES`（詞條）+ `weaponInst`/`effWeapon` 實例系統
  ——這已經是一套 loot 骨架，直接用
- `RARITY_COLOR` 稀有度配色已有
- `1`~`N` 自由位 + 底部快捷列（`castArt` / `drawHotkeyBar`）**整套沿用**，只把成本從 CD 改成怒火
- ~~**要修的既有 bug**：`updateDrops()` 撿取時只帶 `{kind, id, ammo}`，把 `quality` / `enh` 丟掉了~~
  ✅ 已修，並加了探針 `drop-enh`（撿起來的武器必須帶著精良度與詞條）。

---

## 7. 數字要飛

Homeward **完全沒有**傷害飄字（全檔案沒有任何一個實作），而 `CONFIG.juice` 的
`camKick` / `shakeFire` / `shakeHit` / `shakeKill` **全部是 0**——管線接好了卻沒開過。

- **傷害飄字**：一般 / 暴擊（繞背・突襲）/ **招架** / **處決** 各自不同的字級、顏色、拋物線、停留時間
- **處決要有一整套演出**：頓幀 + 鏡頭微推 + 血花 + 大字回血數字
- **聖油要更誇張**：光爆 + 資源噴泉
- **把 juice 參數打開**：震動、後座、頓幀——甜頭一直在那裡沒領
- **地上掉落物發光**：依稀有度的光柱／脈動
- **怒火條要有存在感**：它是玩家全程盯著的東西（滿了會發光＝大絕招可用）
- **層／房間結算的數字**：擊殺數、處決數、招架數

---

## 8. meta：只有兩條軸

跨 run 保留的東西**只有兩樣**：

1. **裝備解鎖 ＝ 是否出現在掉落池中**
   （`UNLOCKS` + `isAvailable()` + `availPool()` 已完整實作，`rollLootWeapon()` 抽的就是 `availPool` 的結果）
2. **不同騎士**（`ORIGINS` / `KNIGHT_TEMPLATES` 已實作）

**Homeward 那 16 條數值升級樹（`UPGRADES`：堅韌/鎮定/鋒銳/疾行…）全部砍掉。**

理由：run 內靠掉落的成長已經夠陡；再疊一層 meta 數值，早期的 run 會變成
「我不是打不好，是數值不夠」——那是反多巴胺的，而且會把前 10 個 run 變成收稅期。

**而且這是 §2.8 難度契約的前提。** 沒有數值 meta，難度目標才能固定在
「零解鎖也打得贏，但完全不能犯錯」。加回數值升級樹＝廢掉那條契約。

> 唯一永遠在增長的東西是**血脈紀年**。它不給數值，只給意義。這是刻意的。

---

## 9. 血脈紀年

### 9.1 機制

- 一個 run ＝**十年**。年份每 run +10（起算年待定，暫用 **聖曆 1200 年**）。
- 每位騎士記錄：`年份` / `名字` / `父名` / `結局`（走到第幾層・死於誰手・死時身上帶什麼・是否已安息）
- 開場**禱文**唸出血脈：
  > 「聖曆一二三〇年。伊爾溫，蓋文之子，蓋文為亞德里克之子……以聖血為印，遣爾入淵。」

### 9.2 命名時機：**在你死的那一刻**

不是開局打字。開局要求打名字，在第 12 次 run 是純摩擦（你只想趕快進去）。

改成：**你倒在地獄裡，畫面問你——「你的兒子叫什麼名字？」**

- 同一個機制，摩擦跑到玩家本來就在看結算的地方
- 情緒強十倍：你在斷氣時為繼承者命名
- **例外**：第一次 run 開局要問一次（初代沒有父親，禱文唸「其父名已佚」）

### 9.2b 你的裝備會回到你兒子手上（使用者定案）

死的那一刻，**身上穿戴的整套**連同名字一起記進紀年（`loadout`）。
那套裝備不會消失——它跟著你的屍體留在地獄裡，變成**被地獄佔領的先祖**（§10）身上的行頭。

**下一代把父親打倒時，那套東西掉回來。**

這條把三個系統一次縫起來：紀年是資料、父輩 boss 是內容、裝備回收是機制。
而且它讓「死掉＝什麼都沒了」變成「死掉＝把家產押在兒子的手上」——
同一件事，情緒完全相反。

### 9.3 禱文會變長

第 20 次 run 的禱文唸的是二十代人。長到爆版就收成：

> 「……蓋文為亞德里克之子，亞德里克為羅蘭之子，**及其先祖十七人**。」

這個儀式感是**免費的**——沒有任何數值，但它是玩家看得見自己走了多遠的唯一刻度。

### 9.4 通關

打贏地獄之王 → **紀年封存**（一份可回看的完整族譜）→ 開新血脈（NG+）。

### 9.5 資料格式（草案）

```jsonc
// localStorage: "tithe_chronicle_v1"   ← 與 meta（解鎖）分開存：
//   前者是故事、後者是進度，生命週期不同（通關會封存紀年，但不該清掉解鎖）
{
  "dynasty": 1,              // 第幾條血脈（通關後開新的）
  "startYear": 1200,
  "line": [
    { "year": 1200, "name": "亞德里克", "father": null,
      "end": {
        "kind": "died", "depth": 3, "killedBy": "howler",
        "loadout": { /* 死時身上的武器/防具/詞條 —— 父輩戰要用，見 §10 */ },
        "laidToRest": false          // 還沒被討回來 → 會在第 3 層以「⚰ 父親」選項出現
      } },
    { "year": 1210, "name": "蓋文", "father": "亞德里克",
      "end": { "kind": "died", "depth": 2, "killedBy": "warden",
               "loadout": { /* … */ }, "laidToRest": true } },   // 已安息
    { "year": 1220, "name": "伊爾溫", "father": "蓋文", "end": null }   // null ＝現役
  ],
  "sealed": []               // 已通關封存的血脈
}
```

---

## 10. 父輩：被地獄佔領的先祖

**失敗沒有懲罰。失敗是在播種。**

死在地獄裡的騎士，身體會被地獄佔據。他還在那裡，帶著他死時身上的東西。

> **你父親帶著你想要的東西，而你必須殺了他才能拿回來。**

### 10.1 他是三選一的其中一個選項

洞口的選項裡出現 **⚰ 父親**，而且照 §5.4 的規矩**全揭露**——直接顯示他身上有什麼。

這代表：**你選擇去見他**，不是被強制塞。你可以連續好幾層都不選他。
「太早被擊敗＝太早出現＝準備不足」的問題因此自動解掉——早出現只代表這個選項提早進菜單。

### 10.2 深度自己校準了強度

他出現在**他死掉的那一層**。所以死在第一層的父親是一場第一層強度的仗，
死在第四層的是一場硬仗。**你不用寫任何難度曲線，它自己對齊了。**

### 10.3 打贏＝安息

打贏 → 拿走他的裝備 → 該代在紀年上標記為 **已安息**，從待討回的名單移除。

- 祖先池不會累積到荒謬（失敗二十次不會有二十場父親戰）
- 紀年多了一條進度軸：**未安息 / 已安息**，那份族譜變成一份有進度的東西
- 「解放被地獄佔據的先祖」這條贖罪線，和教廷／聖血的設定是同一個世界觀，不用額外解釋

### 10.4 他該用玩家的程式碼做

「被佔領的騎士」＝他有騎士的動作組：會 dash、會格擋、會招架、會**處決你**。

- 工程上便宜得離譜：重用玩家單位的行為，不是寫一個全新的 boss AI
- 主題上完全正確：**你在打一個過去的自己**
- 他用的武器就是他死時那把（含詞條）——所以每一場父輩戰的手感都不一樣，
  而且**是你自己上一趟湊出來的那套 build 在打你**

這大概會是全遊戲最好的一場仗，而且是成本最低的一場。

---

## 11. 明確不做的事

承襲自 Homeward 但**刻意砍掉**的系統。細節與程式位置見 [`HARVEST.md`](HARVEST.md)。

| 砍掉 | 理由 |
|---|---|
| 士氣質量模型（整本 `MORALE.md`） | 敵人會潰逃而不是死掉 → 反 loot、反資源迴圈 |
| 潛行 / 識別 / 巡邏 / 搜索 / 聽覺 | 房間是亮的、門是鎖的，沒有可以潛的東西（**視錐與迷霧本身留著**，見 §5.2） |
| 扈從 + 指令輪盤 + 全隊靜默 | 教廷送**一個人**進地獄。這是設定，也是節奏 |
| waypoint 戰術 AI（`scoreNode`/佔點/遲滯） | 封閉房間用不上；換成直接操舵，順便解鎖程序生成 |
| ENTRY / EXTRACT / 撤離 / 追兵 | 出口是房間中央的洞，沒有「撤離」這個概念 |
| 事件 / 導師 / 鐵匠 / 俘虜 / 護送 | run 內經濟改由掉落承擔 |
| 攻擊權配額（`attackTokens`） | 見 §2.7 |
| 負重對移動的懲罰 | 見 §3 |
| meta 數值升級樹（16 條） | 見 §8 |
| 敗騎歸鄉的全部敘事 | 換成血脈紀年 |

**留下的**（曾經考慮砍、現已定案保留）：耐力→改造成**怒火**、**格擋/招架/盾**、
**視錐與迷霧**（預設關）、`1`~`N` **自由位與快捷列**、**大絕招管線**（`useOathSkill` 空殼）。

---

## 12. 待決清單

- [ ] **怒火的定價**（純調值，但是最重要的一組數字）：格擋／奔跑／技能各扣多少、命中／招架／處決各回多少、戰鬥中衰退多快。目標見 §2.3——**中段徘徊、偶爾見底**。
      已實作，數字全在 `CONFIG.wrath`（` 面板即時可調）。第一版：滿 100／衰退 4.5 per s／命中 +5・招架 +18・處決 +36／奔跑 16 per s／格擋 7 + 來襲體幹 ×0.6。**還沒實際玩過，這組數字是待驗的猜測**
- [ ] **奔跑要不要留**：留（吃怒火）vs 取消奔跑鍵、基礎移速直接拉高、只留 dash
- [ ] **怒火減傷做斜坡還是懸崖**：斜坡＝比例減傷（現案）；懸崖＝只有歸零時進入「力竭」（受傷加重／不能格擋）
- [x] **大絕招是什麼**：**全場踉蹌**。半徑內全體滿穿甲重擊 + 體幹全破 + 大擊退——它不替你收人頭，
      它把整個房間變成處決窗，血和怒火還是要你自己走過去一個一個吃回來（＝仍然閉合在 push-forward 上）
- [ ] **聖油的取得**：只在洞口三選一給，還是也會從菁英身上掉？
- [ ] **暗房**要不要進第一版房型池
- [ ] **層王**要不要也能被聖油刪（大概不行，但要明講）
- [ ] **起算年份與地名**：聖曆 1200 是佔位值
- [ ] **通關後**：新血脈的年份重新起算，還是一路累積
- [ ] **武器組 ×2 要不要留**：滾輪換武器組很適合 DOOM 式的多武器切換，但也可能是包袱
- [ ] **怒火的正式名稱**：怒火 / 聖怒（純風味）
