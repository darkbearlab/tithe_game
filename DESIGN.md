# TITHE — 設計文件

> **每十年，教廷抹一名騎士以聖血，送他跨進地獄。他若走到盡頭，人間再安穩十年。**
> **他若死了，你為他的兒子命名。**

單一英雄・俯視・動作 roguelite。承襲 [`darkbearlab/Homeward-`](https://github.com/darkbearlab/Homeward-) 的引擎，
但**不是它的變體**——戰鬥迴圈、流程結構、敘事層全部重寫。要搬什麼砍什麼見 [`HARVEST.md`](HARVEST.md)。

## 目錄

- [1. 三個參照](#1-三個參照)
- [2. 戰鬥：資源迴圈](#2-戰鬥資源迴圈)
- [3. 移動](#3-移動)
- [4. 敵人名冊的設計律](#4-敵人名冊的設計律)
- [5. 流程：層與房間](#5-流程層與房間)
- [6. 成長：掉落即是 boon](#6-成長掉落即是-boon)
- [7. 數字要飛](#7-數字要飛)
- [8. meta：只有兩條軸](#8-meta只有兩條軸)
- [9. 血脈紀年](#9-血脈紀年)
- [10. 明確不做的事](#10-明確不做的事)
- [11. 待決清單](#11-待決清單)

---

## 1. 三個參照

| 面向 | 參照 | 取什麼 |
|---|---|---|
| **戰鬥** | DOOM 2016 | push-forward：資源只從敵人身上來，所以「躲起來回血」不存在，唯一的活路是往前打 |
| **流程** | Hades | 封閉房間、門上先看到獎勵、選的是「你要什麼」不是「你要去哪」 |
| **掉落** | Diablo | 密度。地上一直在發光。但**不繼承它的背包管理**（見 §6） |

不取的東西同樣重要：不取 DOOM 的武器輪盤複雜度（我們是近接為主）、不取 Hades 的厚重 meta（見 §8）、
不取 Diablo 的數值長跑（一個 run 就結束）。

---

## 2. 戰鬥：資源迴圈

### 2.1 核心

DOOM 2016 的核心不是「打得爽」，是**你永遠缺某樣東西，而取得它的唯一辦法是進攻**。
每一個防守動作都在讓你更缺，每一個進攻動作都在補回來。這是整個設計的地基，其他一切都服務於它。

**第一版做兩角，不做三角。**（DOOM 2016 本體是「血 + 彈」兩角；甲是 Eternal 才加的第三角。
先把兩角調到好玩，再考慮要不要第三個。）

| 資源 | 用途 | **唯一**取得方式 |
|---|---|---|
| **聖血**（HP） | 命 | **處決**踉蹌中的敵人 |
| **聖火**（術的能量） | 施術 | **聖油**淨化雜兵（見 2.3） |

沒有回血道具、沒有回血房間、沒有休息整補。**站著不打＝慢性死亡。**

### 2.2 處決（Glory Kill）——最重要的一件事

**這套機制在 Homeward 已經寫好了九成。** 現有的體幹（poise）系統：

- 每次命中扣體幹，扣光＝**體幹破** → 大硬直（`staggerT`）+ 受傷 ×1.6 + 大幅擊退 + 紅色爆擊火花
- 觸發點：`applyPoise()` → `breakPoise()`（`index.html` ~4088–4109）
- 已經有事件掛勾：`Hooks.fire('poiseBroken', { u, by })`
- Homeward 的 README 自己就管這個狀態叫「**處決窗**」

要加的只有最後一哩：

1. 踉蹌中的敵人**視覺上要跳出來**（現在只有一次性火花 → 改成持續的踉蹌標記，遠遠一眼就看得到「那隻可以吃」）
2. 貼近 + 按鍵 → **處決**：短促、無敵、鏡頭微推、頓幀，然後**噴聖血**
3. 處決的回血量與敵人體型掛鉤（雜兵一小口、重甲一大口）
4. 處決期間**不無敵到誇張**——DOOM 給短無敵是為了鼓勵你衝進人堆，我們沿用，但窗要短

> **設計律：處決是唯一的回血手段。** 一旦破例（掉落回血包、術能回血），整個 push-forward 就塌了。
> 術可以回血——但術要燒聖火，聖火要靠聖油，聖油要靠找到雜兵。迴圈仍然閉合在敵人身上。

### 2.3 聖油（Chainsaw 的位置）

DOOM 的電鋸妙在它是**一顆「保證拿到彈藥」的按鈕**，由一種獨立的稀有燃料驅動。它讓「彈盡」永遠不是死局，
只要場上還有一隻雜兵。

- **聖油**是有限的（幾管，戰鬥中緩慢回充或從特定敵人掉落）
- 對**雜兵**使用＝立即淨化（秒殺，不管血量）+ 噴一大波聖火
- 對**大隻的**使用＝無效或效率極差（不能拿來當萬用秒殺鍵）
- 所以雜兵不是麻煩，是**行走的補給站**——這反過來要求名冊裡永遠有雜兵在場（見 §4 律則 5）

### 2.4 耐力：**刪除**

Homeward 把耐力寫成「戰鬥的核心語言」（揮擊/格擋/奔跑都要錢，歸零＝揮不動）。
那套語言的意思是「**別亂打**」。TITHE 的語言是「**打就是活**」。兩者不能共存。

耐力整條砍掉：`u.stam` / `spendStam` / `stamCost` / 耐力條 / 盾格耗耐力 / 奔跑耗耐力，全部拔除。
節奏的煞車改由**動作本身的前後搖**承擔（重武器揮得慢就是慢），那是位置正確的煞車——
它懲罰的是「選錯時機」，不是「攻擊得太多」。

### 2.5 攻擊權（`attackTokens`）：**刪除**

現有機制是「同時只有 2 隻近接敵能進攻，其餘在外圍繞行」。那是黑暗騎士／刺客教條式的近戰禮貌，
用來讓 1 打多在**慢節奏**下可解。

DOOM 的壓力來源相反：**所有東西都在攻擊你，同時**。之所以可解，是因為每一發都**看得見、躲得掉**。
所以：拿掉攻擊權配額，壓力改由「可見可躲的投射物 + 有預告的近接突進」承擔。

現有的**攻擊預告**系統（近接扇形 / 遠程長方形彈道走廊，填滿的瞬間＝落下的瞬間）正是為這件事寫的，
全部留下並且**加強**——它從「輔助資訊」升格成「戰鬥的主要語言」。

### 2.6 盾與格擋

待決（見 §11）。傾向：**沒有格擋鍵**。DOOM 沒有防禦鍵，防禦＝移動。
盾若要留，改成被動（正面減傷）或改成攻擊性道具（盾擊＝製造踉蹌 → 接處決），不做「按住不動」。

---

## 3. 移動

「絲滑」不是形容詞，是四個具體數字：

| 項目 | Homeward 現況 | TITHE |
|---|---|---|
| 基礎移速 | 110 px/s（註解寫「戰術化：放慢」） | **顯著調高**（先試 190–220） |
| 整體節奏 `CONFIG.tempo` | **1.2（＝刻意拉長 20%）** | **回到 1.0 或更低**（0.85–0.9） |
| 加速度 / 摩擦 | 無（瞬間到達目標速度） | 保持瞬間感，但加**極短的減速尾巴**讓轉向不黏 |
| 衝刺 | Shift 長按、吃耐力、被負重懲罰 | **改成 dash**：短距離瞬移、CD 制、**不吃任何資源**、有 i-frame |

dash 是這一版最重要的新動作。它是你唯一的防禦，而且**免費**——因為 DOOM 的設計是「防禦不該有成本，
進攻才有回報」。負重對移動的懲罰全部拿掉（`sprintMult` / `carriedWeight` 那條線砍掉）。

---

## 4. 敵人名冊的設計律

> 「不是無雙割草，而是不同個性鮮明的敵人穿插出現。」

這句話展開就是 DOOM 的敵人設計律。訂成硬規則，任何新敵人都要通過：

1. **每一隻問一個不同的問題**，而且有一個「正確答案」（某個武器、某個位置、某個時機）
2. **每一隻有唯一的空間要求**（逼你站到某處）**或**唯一的時間要求（逼你在某刻動）
3. **沒有任何一隻可以被「站著平砍」解決**——如果可以，牠不該存在
4. **同時在場 8–15 隻，不是 40 隻**。壓力來自**組合**，不是數量
5. **每一隻都是資源**。沒有純麻煩的敵人；擋路的東西同時是補給

律則 4 對引擎是好消息：不必為湧怪重寫，現有的單位規模就夠。

### 初版名冊（草案，7 隻）

| # | 名 | 行為 | 牠問的問題 | 正確答案 |
|---|---|---|---|---|
| 1 | **膿仔** | 慢、弱、成群、不痛 | 無（牠不是威脅） | **忽略牠，直到你缺聖火**——牠是聖油的目標 |
| 2 | **撲咬者** | 極快直線突進，有預告線，撲空後有硬直 | 你能不能在移動中側閃 | dash 或走位側移，然後吃牠的硬直 |
| 3 | **投火者** | 遠程拋物線，落點預告圈 | 你能不能**不站定** | 持續移動；優先清掉（牠讓你不能站著處決） |
| 4 | **盾衛** | 正面免疫（沿用現有裝甲第一層 + 盾兵反射舉盾） | 你能不能在混戰中繞到背後 | 繞背（現有 `flank` 無視雙層裝甲的機制直接可用） |
| 5 | **嚎叫者** | 不攻擊，但持續強化／召喚附近敵人 | 你能不能**放棄眼前的目標**去處理優先級 | 立刻衝過去殺掉，即使代價是背對別人 |
| 6 | **重甲** | 高血高甲、慢、近身重擊、擊退強 | 資源管理：你打不動牠 | 破甲手段（鎚類 / 高 pierce 的術）；或先清場再耗 |
| 7 | **裂體者** | 死時分裂成兩隻小的（小的分裂成膿仔） | 清場順序：你要在什麼時機殺牠 | 留到最後，或用範圍一次收乾淨 |

**Homeward 現有兵種可直接映射**（`UNIT_CLASSES` / `ENEMY_TYPES` 的表結構原樣沿用，只換資料）：
`levy`→膿仔、`wolf`→撲咬者、`archer`/`arbalest`→投火者、`shieldman`→盾衛、`manatarms`/`serjeant`→重甲。
嚎叫者與裂體者是新的行為，要新寫。

### AI 的形狀要換

現有 AI 是**戰術 waypoint 制**（`scoreNode` 依標籤/暴露/側翼評分選點、佔點 `reservation`、遲滯 `hysteresis`、
巡邏認領、搜索焦點）。那是為「潛行 + 掩體 + 少量敵人」寫的，在封閉房間裡完全用不上。

換成**直接操舵**（steering）：朝目標移動 + 避開彼此 + 各兵種自己的位移花招（繞圈 / 突進 / 保持距離）。
這一換順便解掉「敵人不做空間尋路、只能走手刻 waypoint 圖」的限制——**房間可以程序生成了**。

---

## 5. 流程：層與房間

### 5.1 房間

- **封閉房間**。進門 → 門鎖上 → 清場 → 門開。
- **沒有撤離點、沒有進出點、沒有潛行、沒有迷霧。** 房間是亮的，你看得到所有東西。
  （Homeward 的 ENTRY/EXTRACT/`accessPoints`/撤離/追兵/視錐/迷霧整套拔除——見 `HARVEST.md`。）
- 房間大小約現有地圖的 1/2 ~ 2/3，重點是**沒有死角、沒有可以龜的地方**。

### 5.2 門上先顯示獎勵

Hades 的關鍵不是「分岔」，是**你在踏出去之前就知道那扇門後面給什麼**。
選擇因此是「我這一趟要什麼」，不是「我賭哪一邊」。

門的獎勵圖示（草案）：⚔ 裝備三選一 · 🔥 聖油/聖火補充 · 💀 精英房（獎勵更好） · ✝ 祭壇 · 👑 層王

### 5.3 層（biome）

四層地獄，每層 8–12 房 + 層王。層與層之間換配色、換敵人組合權重、換場景元素。
最後是**地獄之王**。

### 5.4 引擎上怎麼接

現有 `genRoadmap()` 的圖生成 + `validateRoadmap()` 連通性檢查可以留（它就是一張有向圖），
但**表現形式**要換：從「一次看到 7 欄的全局路線圖」變成「一次只看到眼前的兩三扇門」。

---

## 6. 成長：掉落即是 boon

### 6.1 張力

「裝備噴得和垃圾一樣多」和「DOOM 的節奏」直接衝突：**在戰鬥中停下來比較裝備數值會把節奏殺死。**
Hades 用三選一的即時 boon 解決，Diablo 用暫停背包解決。我們不能用 Diablo 的解法。

### 6.2 建議方案：兩層分離

| 層 | 在哪 | 是什麼 | 玩家動作 |
|---|---|---|---|
| **爽度層** | 戰鬥中，滿地都是 | 聖血碎、聖火碎、聖油、暫時性增益 | **走過去就吃**，零決策、零暫停 |
| **決策層** | 房間清完，門口 | 裝備（武器/防具/術），**三選一** | 停下來看，但戰鬥已經結束了 |

這樣「噴得像垃圾」的爽度發生在戰鬥裡（滿地發光、數字亂飛），而「這一趟我在 build 什麼」的決策
集中在門口——**兩者都不打斷對方**。

### 6.3 沿用現有資產

- `WEAPON_QUALITY`（精良度 1–5 ＝強化格數預算）+ `AFFIXES`（詞條）+ `weaponInst`/`effWeapon` 實例系統
  ——這已經是一套 loot 骨架，直接用
- `RARITY_COLOR` 稀有度配色已有
- **要修的既有 bug**：`updateDrops()` 撿取時只帶 `{kind, id, ammo}`，把 `spawnDrop()` 帶上的
  `quality` / `enh` 丟掉了（`enterRest()` 又在讀它們）→ 撿起來的武器精良度與詞條會歸零。
  搬過來的時候一併修掉。

---

## 7. 數字要飛

Homeward **完全沒有**傷害飄字（全檔案沒有任何一個實作），而 `CONFIG.juice` 的
`camKick` / `shakeFire` / `shakeHit` / `shakeKill` **全部是 0**——管線接好了卻沒開過。

要做的：

- **傷害飄字**：一般 / 暴擊（繞背・突襲）/ 處決 各自不同的字級、顏色、拋物線、停留時間
- **處決要有一整套演出**：頓幀 + 鏡頭微推 + 血花 + 大字回血數字
- **把 juice 參數打開**：震動、後座、頓幀——甜頭一直在那裡沒領
- **地上掉落物發光**：依稀有度的光柱／脈動
- **層/房間結算的數字**：擊殺數、處決數、最高連段

---

## 8. meta：只有兩條軸

跨 run 保留的東西**只有兩樣**：

1. **裝備解鎖 ＝ 是否出現在掉落池中**
   （`UNLOCKS` + `isAvailable()` + `availPool()` 已完整實作，`rollLootWeapon()` 抽的就是 `availPool` 的結果）
2. **不同騎士**（`ORIGINS` / `KNIGHT_TEMPLATES` 已實作）

**Homeward 那 16 條數值升級樹（`UPGRADES`：堅韌/鎮定/鋒銳/疾行…）全部砍掉。**

理由：run 內靠掉落的成長已經夠陡；再疊一層 meta 數值，早期的 run 會變成
「我不是打不好，是數值不夠」——那是反多巴胺的，而且會把前 10 個 run 變成收稅期。

> 唯一永遠在增長的東西是**血脈紀年**（§9）。它不給數值，只給意義。這是刻意的。

---

## 9. 血脈紀年

### 9.1 機制

- 一個 run ＝**十年**。年份每 run +10（起算年待定，暫用 **聖曆 1200 年**）。
- 每位騎士記錄：`年份` / `名字` / `父名` / `結局`（走到第幾層・死於誰手・或通關）
- 開場**禱文**唸出血脈：
  > 「聖曆一二三〇年。伊爾溫，蓋文之子，蓋文為亞德里克之子……以聖血為印，遣爾入淵。」

### 9.2 命名時機：**在你死的那一刻**

不是開局打字。開局要求打名字，在第 12 次 run 是純摩擦（你只想趕快進去）。

改成：**你倒在地獄裡，畫面問你——「你的兒子叫什麼名字？」**

- 同一個機制，摩擦跑到玩家本來就在看結算的地方
- 情緒強十倍：你在斷氣時為繼承者命名
- **例外**：第一次 run 開局要問一次（初代沒有父親，禱文唸「其父名已佚」或「無父之子」）

### 9.3 禱文會變長

第 20 次 run 的禱文唸的是二十代人。長到爆版就收成：

> 「……蓋文為亞德里克之子，亞德里克為羅蘭之子，**及其先祖十七人**。」

這個儀式感是**免費的**——沒有任何數值，但它是玩家看得見自己走了多遠的唯一刻度。

### 9.4 記下死因與死處

「亞德里克，死於第三層，為嚎叫者所噬」——紀年表會自己長成一份讀物。

延伸（低成本高回報，可選）：**父親的屍骸成為下一 run 場景裡的一座祭壇**。你會走過你父親倒下的地方。

### 9.5 失敗的十年怎麼算

**建議：失敗＝那十年沒買到，而且人間看得出來變糟。**

- 開場禱文的**措辭**隨連續失敗次數變化（莊嚴 → 焦急 → 絕望）
- 地獄本身變得更擁擠（敵人組成權重微調）

⚠️ **但要小心死亡螺旋**：連續失敗 → 遊戲變難 → 更容易失敗，那會直接勸退。
建議**只做表現、不做數值**，或數值幅度小到玩家感覺得到氛圍卻感覺不到難度。列入待決（§11）。

### 9.6 通關

打贏地獄之王 → **紀年封存**（一份可回看的完整族譜）→ 開新血脈（NG+，年份重新起算或延續）。

### 9.7 資料格式（草案）

```jsonc
// localStorage: "tithe_chronicle_v1"
{
  "dynasty": 1,              // 第幾條血脈（通關後開新的）
  "startYear": 1200,
  "line": [
    { "year": 1200, "name": "亞德里克", "father": null,
      "end": { "kind": "died", "depth": 3, "killedBy": "howler" } },
    { "year": 1210, "name": "蓋文", "father": "亞德里克",
      "end": { "kind": "died", "depth": 2, "killedBy": "warden" } },
    { "year": 1220, "name": "伊爾溫", "father": "蓋文",
      "end": null }                                    // null ＝現役
  ],
  "sealed": []               // 已通關封存的血脈
}
```

`meta`（解鎖）與 `chronicle`（紀年）**分開存**——前者是進度，後者是故事，兩者的生命週期不同
（通關會封存紀年，但不該清掉解鎖）。

---

## 10. 明確不做的事

承襲自 Homeward 但**刻意砍掉**的系統，以及一句話理由。細節與程式位置見 [`HARVEST.md`](HARVEST.md)。

| 砍掉 | 理由 |
|---|---|
| 耐力 | 一個「叫你別攻擊」的資源，與 push-forward 直接對立 |
| 士氣質量模型（整本 `MORALE.md`） | 敵人會潰逃而不是死掉 → 反 loot、反資源迴圈 |
| 潛行全套（視錐/迷霧/識別/聽覺/巡邏/搜索） | 房間是亮的、門是鎖的，沒有可以潛的東西 |
| 扈從 + 指令輪盤 + 全隊靜默 | 教廷送**一個人**進地獄。這是設定，也是節奏 |
| waypoint 戰術 AI（`scoreNode`/佔點/遲滯） | 封閉房間用不上；換成直接操舵，順便解鎖程序生成 |
| ENTRY / EXTRACT / 撤離 / 追兵 | Hades 式房間沒有「撤離」這個概念 |
| 事件 / 導師 / 鐵匠 / 俘虜 / 護送 | run 內經濟改由掉落承擔 |
| meta 數值升級樹（16 條） | 見 §8 |
| 敗騎歸鄉的全部敘事 | 換成血脈紀年 |

---

## 11. 待決清單

- [ ] **盾與格擋**：完全拿掉？被動減傷？還是改成攻擊性的盾擊（製造踉蹌 → 接處決）？
- [ ] **第三個資源（甲）**：第一版先做兩角，之後要不要補？
- [ ] **流程呈現**：純 Hades（一次只看到眼前的門）vs 保留全局圖但只揭示前方兩步？
- [ ] **連段計數器（combo meter）**：DOOM 沒有，但多巴胺想要。要不要？如果要，它獎勵什麼？
- [ ] **失敗的十年**：只做表現，還是也動數值？（死亡螺旋風險，見 §9.5）
- [ ] **起算年份與地名**：聖曆 1200 是佔位值
- [ ] **通關後**：新血脈的年份重新起算，還是一路累積下去？
- [ ] **武器組 ×2 要不要留**：現有的滾輪換武器組很適合 DOOM 式的多武器切換，但也可能是包袱
